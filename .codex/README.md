# Codex settings

Common settings which are copied (if `.codex/config.toml` does **not** exist already in destination) to the repo root on devcontainer startup.

Since this config supercedes home-directory sourced config, only add items which are project-specific, _not_ user-preference based.

Things to add here includ:

- related general guidance about using common project tools (ex. kubernetes)
- MCP servers
- corporate code style guidance
