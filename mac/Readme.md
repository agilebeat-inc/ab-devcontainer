# Mac mounting trick

For MAC you can mount root file system as a folder on root to have same paths as in a docker container.
To do that copy synthetic.cnf file to /etc folder.

```cp synthetic.conf /etc```
